# Mirona Industry Demo

A simple demo website for Mirona Industry PLC.

## Quick Start

1. Install dependencies: `pip install -r requirements.txt`
2. Run server: `python manage.py runserver`
3. Visit: `http://localhost:8000`

## Deployment

Connected to Render - auto deploys on git push.

**Note:** This is a demo site with sample content only.